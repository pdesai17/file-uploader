import { runWorkflow } from "./run-Drs-gaFc.js";

//#region src/hono.ts
const createHonoHandler = (workflow, getStart, stopEvent, wrapStopEvent) => {
	if (!wrapStopEvent) wrapStopEvent = (c, stop) => {
		return c.json(stop);
	};
	return async (c) => {
		const stop = await runWorkflow(workflow, await getStart(c), stopEvent);
		return wrapStopEvent(c, stop.data);
	};
};

//#endregion
export { createHonoHandler };
//# sourceMappingURL=hono.js.map